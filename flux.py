# import the catalog file, use filename to acquire the correct image files

# set up columns for new aperture photometry measurements

# for loop iterating over each row in the catalog

    # load in this star's ellipse properties from the catalog
    
    # create all apertures





ellipse_flux = MaskedColumn(length=len(catalog), name='ellipse_flux', mask=True)
